import PuzzleModal from '../PuzzleModal/PuzzleModal'

const LevelNine = () => {
	return (
		<PuzzleModal apiId='nine' nextLevelLocation='level10' labelForLevel='9' />
	);
};

export default LevelNine;
