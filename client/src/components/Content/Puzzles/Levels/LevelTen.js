import PuzzleModal from '../PuzzleModal/PuzzleModal'

const LevelTen = () => {
	return (
		<PuzzleModal apiId='ten' nextLevelLocation='level1' labelForLevel='10'/>
	);
};

export default LevelTen;
