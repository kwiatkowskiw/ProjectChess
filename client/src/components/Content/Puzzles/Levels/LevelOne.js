import PuzzleModal from '../PuzzleModal/PuzzleModal'

const LevelOne = () => {
	return (
		<PuzzleModal apiId='one' nextLevelLocation='level2' labelForLevel='1' />
	);
};

export default LevelOne;
